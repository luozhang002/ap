# pnpm 默认把依赖放在隔离的 node_modules 结构中，TypeScript 在解析「隐式类型库」时
# 往往找不到嵌套在深层路径里的 @types/*，从而连续报：
#   Cannot find type definition file for 'estree' | 'json-schema' | 'json5' | ...
# 提升为与 npm 相近的扁平结构后，tsc 能稳定解析所有传递依赖里的 @types。
# 见：https://pnpm.io/npmrc#shamefully-hoist
shamefully-hoist=true
