1. AP目录下，我后面希望建立两个工程，一个工程的名字是oms，内部运营管理系统，主要用于客户数据导入，一个是用户账号权限管理。

另外一个系统采用叫 crm，客户关系管理系统，这个系统暂时用H5写，是一个移动端的项目。

两个系统的技术架构都采用react + nextjs的方式，数据库用同一套数据库，但是技术栈是React + nextjs + mysql这一套。数据库可以用primsa连接。帮我初始化两个工程，一个端口3000，一个3001，写一些简单的内容


2. 数据库mysql -uroot -p 123456

3. OK，接下来我们开发企业管理模块，企业管理模块的数据是支持我们从excel里面导入的，每一个excle里面有三个sheet，数据导入后会列表现实，上面支持列表筛选，数据库中的字段要和表格对应，因为有三个sheet，字段基本一样，需要有一个字段区分三个不同的 sheet，一个sheet是代表一类，一个sheet代表非常规名单，一个代表接力棒，每一个里面都有下面的字段："下发时间 (不可改)"	提供时间	地区	所在区	ccif	客户名称	下发的地址	地址核验标签（行方录入）	额度	当前最优套餐价格	新/存 客	是否为MGM客户	最近一次授信成功时间	锁期时间	当前套餐还款方式	参考价	我行参考历史价	是否可用折扣券	预计金额违约率是否大于9	负责人	分中心负责人	法人姓名	联系电话	实际经营地址	首次联系时间	首次接通时间	最新一次联系时间（多次联系的填写）	最新一次接通时间	最近一次上门时间	联系次数	确认客户是否有意愿	未触达原因	预约上门时间	label	是否实际上门	实际上门时间	是否见到客户	是否疑似风险客户（客户经理独立意见）	渠道	电销备注	接力棒标签	影像资料	是否完成地址核验	登记时间	今天登记次数	父记录	父记录 2	联系/走访情况备注，帮我设计下数据库，同时支持excle导入，每次excel三个sheet，目前可以假设三个sheet的字段都一样，后面可能部分会多些字段 ,帮我设计下整个企业管理模块的数据库和功能页面

4. 同时做好备注：**当前 Excel 数据模板与数据库表结构是阶段性设计**，后续业务若调整模板（列名、列顺序、新增/删减字段等），需要同步评估并可能修改：Prisma schema、导入解析（如 `enterprise-headers` / `enterprise-import`）、序列化与 API、列表与导出 UI。请在相关代码与 `oms/README.md` 中保留此类维护说明。

5. @oms @crm 因为excel的模板改了，之前OMS上传excel后区分三种不同的sheet落库存，客户经理登陆crm系统根据是否实际上门来判断哪些是自己拜访的企业。

sheet模板有动，整个数据库的设计和代码处理逻辑也会有改动，模板就是后面这种文件@script/testdata.xlsx ，具体可以看这个里面三个sheet的分析@script/sheet-compare-result.md ,以前的数据表中没用的表数据可以删除，也可以重建表，标的每个字段都要做好描述

6. 现在有个问题我访问：http://localhost:3001/dashboard/visit-map可以定位到当前位置，但是我访问：http://10.10.8.76:3001/dashboard/visit-map  就定位不到当前位置？为什么呢 